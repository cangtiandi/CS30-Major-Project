
function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}

class Tetris {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  blocks() {
    
  }
}

